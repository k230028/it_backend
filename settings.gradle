// [폐쇄망] 내부 Nexus 사용 시 아래 pluginManagement 블록의 주석을 해제합니다.
// 플러그인(org.springframework.boot 등)은 기본적으로 Gradle Plugin Portal에서 받으므로,
// Nexus 그룹 저장소에 Gradle Plugin Portal 프록시(https://plugins.gradle.org/m2/)가
// 멤버로 포함되어 있어야 합니다. 이 블록은 반드시 파일 최상단에 위치해야 합니다.
// pluginManagement {
// 	repositories {
// 		maven {
// 			// 방식 A: 내부 Nexus (마커 POM 포함 전체 아티팩트가 업로드되어 있어야 함)
// 			// url = 'http://10.6.65.151:20080/repository/maven-releases/'
// 			// allowInsecureProtocol = true   // http이므로 필수
//
// 			// 방식 B: 반입한 로컬 폴더 저장소 (make-local-maven-repo.ps1 산출물)
// 			url = 'file:///C:/maven-repo'
//
// 			// Gradle Module Metadata(.module) 미요청 — POM만으로 해석.
// 			// .module은 반입 신청 불가 유형이므로 저장소에 없어도 동작하도록 필수 유지.
// 			metadataSources {
// 				mavenPom()
// 				artifact()
// 				ignoreGradleMetadataRedirection()
// 			}
// 		}
// 	}
// }

rootProject.name = 'it'
