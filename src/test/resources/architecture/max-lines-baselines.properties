# 백엔드 운영 소스 800줄 초과 파일의 동결선 (CQ-01 / Wave D-1)
#
# 규칙
#   1. 여기 적힌 파일의 실제 줄 수는 기준값과 정확히 일치해야 한다 (증가·감소 모두 실패).
#   2. 여기 없는 src/main/java 파일이 800줄을 넘으면 실패한다.
#   3. 여기 적힌 경로가 실재하지 않으면 실패한다.
#
# 기능 추가로 불가피하게 초과하면 같은 PR에서 동등 이상 분량을 추출해 상쇄한다.
# 분해로 줄었으면 같은 커밋에서 기준값을 실측값으로 낮추고, 800줄 이하가 되면 항목을 지운다.
#
# 기준값 상향은 허용된 해소 수단이 아니다. 신규 항목 추가도 게이트를 통과시키는 기본 수단이 아니며,
# 불가피한 경우에만 리뷰어 승인을 받고 PR에 사유를 남긴다.
#
# 경로는 src/main/java 기준 상대 경로이며 구분자는 항상 '/'다.
#
# 기준 시점: 2026-08-05 / backend 9b5104e9
#
# 측정 정의: Files.readAllLines(path, UTF_8).size()
# PowerShell `Get-Content | Measure-Object -Line`은 쓰지 않는다 — 빈 문자열의 줄 수를 0으로 계산해
# 파일의 빈 줄이 통째로 누락되기 때문이다(인코딩·BOM과는 무관).
# 스팟체크는 `[System.IO.File]::ReadAllLines($p).Length` 또는 `(Get-Content $p).Count`를 쓴다.
com/kdb/it/domain/budget/project/dto/ProjectDto.java=1005
com/kdb/it/domain/council/dto/CouncilDto.java=916
